// variavel var
/*
let forma = "retangulo";
let altura = 3;
let comprimento = 4;
let area;

if (forma === "retangulo"){
    area = altura * comprimento;
} else {
    area = (altura *comprimento)/2;
}

console.log(area)
*/

/*
const forma ='quadrado';
const altura = 5;
const comprimento =7;
const area; // ERRO -> não é possivel criar uma variavel const sem atribuir valor 
if (forma === 'quadrado'){
    const area = altura * comprimento;
} else {
    area = (altura *comprimento)/2;
}
*/
let forma ='quadrado';
let altura = 5;
let comprimento =7;
let area; 
if (forma === 'quadrado'){
    area = altura * comprimento;
} else {
    area = (altura *comprimento)/2;
}

console.log(area)

